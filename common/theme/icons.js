import React, {
  // PropTypes
} from 'react'
import FontIcon from 'material-ui/FontIcon'

export const Chat = (props) => <FontIcon {...props} className="material-icons icon-chat">chat</FontIcon>
export const VolumeOff = (props) => (
  <FontIcon {...props} className="material-icons icon-volume-off">volume_off</FontIcon>
)
